// import {createContext} from "react"
// export const noteContext=createContext();
//   default noteContext;

import { createContext } from "react";

const NoteContext = createContext();

export default NoteContext;

// ham react devta se bole ke hame context use karna hai .. to unhone kaha ke pahle apne liye(khud ka) context banao mere diye hue method se then export karo yaha se